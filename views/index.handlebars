<h1>BURGERS</h1>

<h2>Burgers that have not been eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/cat-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/cat-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Order a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">What type of burger would you like?</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten<br>
    <input type="radio" name="eaten" value="0"> Not eaten
  </div>

  <button type="submit">Order burger</button>
</form>
